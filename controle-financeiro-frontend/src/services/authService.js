/**
 * ESTE ARQUIVO FOI REMOVIDO.
 *
 * authService.js era uma duplicata de auth.js com comportamento ligeiramente diferente
 * (armazenava o token automaticamente no localStorage sem considerar a opção "lembrar-me").
 *
 * Use auth.js no lugar:
 *   import { login, register, logout, isAuthenticated } from './auth';
 *
 * Se algum componente importava authService.js diretamente, troque o caminho.
 */
